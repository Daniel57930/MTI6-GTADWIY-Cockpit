/** Next.js config for MTI6-GTADWIY Cockpit scaffold */
const nextConfig = {
  reactStrictMode: true,
  env: {
    // Runtime environment variables come from process.env set in Vercel
  }
};

module.exports = nextConfig;